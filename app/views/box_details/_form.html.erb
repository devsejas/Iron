<%= form_with(model: box_detail, local: true) do |form| %>
  <% if box_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(box_detail.errors.count, "error") %> prohibited this box_detail from being saved:</h2>

      <ul>
      <% box_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :box_id %>
    <%= form.number_field :box_id %>
  </div>

  <div class="field">
    <%= form.label :payment_id %>
    <%= form.text_field :payment_id %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
